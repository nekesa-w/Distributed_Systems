version: '3'
services:
  load_balancer:
    build: ./load_balancer
    ports:
      - "5000:5000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    privileged: true
    networks:
      - load_balancer_network

networks:
  load_balancer_network:
    driver: bridge